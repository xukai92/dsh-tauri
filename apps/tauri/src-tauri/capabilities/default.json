{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (no custom IPC yet; core only).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
