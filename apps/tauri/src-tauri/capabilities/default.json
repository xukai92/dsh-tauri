{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core only; the content window (relabeled per connection) and the remote-connect prompt window.",
  "windows": ["main*", "remote"],
  "permissions": ["core:default"]
}
