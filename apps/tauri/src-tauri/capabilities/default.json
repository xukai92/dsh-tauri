{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: core only; the main window and the remote-connect prompt window.",
  "windows": ["main", "remote"],
  "permissions": ["core:default"]
}
